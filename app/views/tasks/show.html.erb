<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @task.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @task.description %>
</p>

<p>
  <strong>Completed:</strong>
  <%= @task.completed %>
</p>

<p>
  <strong>User:</strong>
  <%= @task.user.email %>
</p>

<% if current_user.has_role? :student %>
  <%= form_for @task, url: task_mark_complete_path(@task), html: { method: :patch } do |f| %>
    <%= f.submit 'Complete' %>
  <% end %>

<!-- <form method="post" action="/tasks/mark_complete">
  <input type="submit" value="Complete"/>
</form> -->
<% end %>

<%= link_to 'Edit', edit_task_path(@task) %> |
<%= link_to 'Back', tasks_path %>
