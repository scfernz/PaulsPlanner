
<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <p>
    <%= f.label :title %>*<br>
    <%= f.text_field :title %>
    </p>
  </div>
  <div class="field">
    <p>
    <%= f.label :description %><br>
    <%= f.text_area :description %>
    </p>
  </div>

  <div class="field">
    <p>
    <%= f.label :user_id %><br>
    <%= collection_select(:task, :user_id, User.with_role(:student), :id, :name) %>
    </p>
  </div>

  <div class="actions">
    <p>
    <%= f.submit create: 'Create Task', update: 'Update Task', :class => "btn btn-success" %>
  </p>
  </div>
<% end %>
