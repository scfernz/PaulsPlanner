<h1>My Meetings</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Date</th>
      <th>Location</th>
    </tr>
  </thead>

  <tbody>
    <% @meetings.each do |meeting| %>
      <tr>
        <td><%= link_to meeting.date, meeting %></td>
        <td><%= meeting.location %></td>
      </tr>
    <% end %>
  </tbody>
</table>
