<p id="notice"><%= notice %></p>

<h1>Listing Students</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Cohorts</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <!-- TODO: change so that it displays students -->
    <% User.with_role(:student).each do |user| %>
        <tr>
          <td><%= user.name %></td>
          <!-- TODO: provide link to add student to cohort -->
        </tr>
      <% end %>
  </tbody>
</table>
